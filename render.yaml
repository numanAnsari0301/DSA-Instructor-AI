services:
  - type: web
    name: dsa-instructor-ai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run dsa.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: GEMINI_API_KEY
        sync: false
